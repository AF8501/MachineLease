<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gyx.jixiezulin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MachineLease.MachineLeaseActivity"
            android:windowSoftInputMode="stateHidden"
            android:screenOrientation = "nosensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".EquipmentList.EquipmentListActivity"
            android:windowSoftInputMode="stateHidden"
            android:screenOrientation = "nosensor">

        </activity>

        <activity android:name=".BrandList.BrandListActivity"
            android:screenOrientation = "nosensor"/>
        <activity android:name=".ModelList.ModelListActivity"
            android:screenOrientation = "nosensor"/>
        <activity android:name=".PostContracts.PostContractsActivity"
            android:screenOrientation = "nosensor"/>

    </application>

</manifest>